from enum import StrEnum

class MediaActionMode(StrEnum):
    COMPATIBILITY_FIRST = "Maximize compatibility across apple devices"
    QUALITY_FIRST = "Preserve Quality First (may not work for older apple devices)"
